name: errbit
summary: errbit is an open source exception catcher
maintainer: Charles Butler <chuck@dasroot.net>
description: |
  Will deploy Errbit, Nginx, MongoDB and Sendmail by default.
categories:
  - application
provides:
    airbrake:
        interface: airbrake
requires:
    mongodb:
        interface: mongodb
        optional: true
peers:
  errbit:
    interface: errbit
